import 'package:flutter/material.dart';

class HyougoPage extends StatelessWidget {
  const HyougoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Hyougo Page')),
      body: const Center(child: Text('Welcome to tot to Hyougo!')),
    );
  }
}
